/* import Character from '../js/character';

test('character creation Unit', () => {
        const unit = new Character('Red', 'Hacker', 50, 1, 40, 10);
        const result = {
          name: 'Red',
          type: 'Hacker',
          health: 50,
          level: 1,
          attack: 40,
          defence: 10,
        };
        expect(unit).toEqual(result);
      });
       */